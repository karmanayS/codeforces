zod schema req.body doesnt have visible , hidden tests and understand user in zod schema
cron job to delete data permanently after 30 days ?
If we create question then we do not need id but when we fetch all questions, we do need id
Submissions table prisma 
resend otp option
admin email list hardcoded for now
authorization checks on routes ie only signed in people should be able to access routes and admin page should only be accessed by admin
optimise frontend ==> react query and s30 1 advance frontend lecture
optimise backend ?
category shouldnt be a string in prisma schema
make server components of most of the things like problems and just import them in client components
code editor button colors 
signin route early return ki jagah use prisma catch error if email already exists ==> this saves a db call
problem acceptance rate hardcoded
use params prop insstead of usePathname hook to fetch problems page and problemID
languages hardcoded in userRouter/submission/:questionID
fix status filter in problems list page

add time taken and memory in submissions list 

do not allow submission with the same source code again ==> in the backend the prisma.create will fail if same source code is there because source code is a unique field, this might already be a good fix but it doesnt tell the user that u have made a submission already with the same source code and it just throws a generic error for now

